\documentclass[journal,twocolumn]{IEEEtran}
\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage[vietnamese,english]{babel}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{float}
\usepackage{makecell}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage[left=0.55in, right=0.55in, top=0.70in, bottom=0.70in, columnsep=0.32in]{geometry}

% Cấu hình màu sắc liên kết chuẩn phong cách bài báo IEEE (IEEE Blue)
\definecolor{IEEEblue}{RGB}{0,51,102}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=IEEEblue,
    citecolor=IEEEblue,
    urlcolor=IEEEblue,
    pdfborder={0 0 0}
}

% =====================================================================
% RESTRUCTURED VERSION (Round 1: structural reorganization only).
% Section map: I. Introduction | II. Related Work | III. Materials and
% Methods | IV. Results and Discussion | V. Conclusion and Future Work.
% Content moved/merged with minimal rewriting. Deeper content revisions
% are marked inline with "% TODO-R2:" comments for the next round.
% =====================================================================

\begin{document}

% Tiêu đề bài báo được tinh chỉnh cỡ chữ \Large \bfseries nhỏ nhắn, sang trọng và thu hút
\title{\Large \bfseries Urban Traffic Perception and Multi-Horizon Graph Forecasting: A Two-Stage Framework via ConvNeXt Direct Regression and Temporal Attention STGCN}

\author{Viet-Anh Le, Dung Hoang Viet, Khanh Nguyen-Trong*\\
\textit{Intelligent Computing for Sustainable Development Laboratory (IC4SD) \& Faculty of Information Technology \\ Posts and Telecommunications Institute of Technology, Hanoi, Vietnam}\\
}


\maketitle

% TODO-R2: Abstract kept verbatim this round. Revisit "superior" claims after
% significance testing; consider adding MAPE and softening the improvement claim
% over STGCN (delta = 0.02 MAE vs. std ~0.011-0.015).
\begin{abstract}
Urban traffic congestion presents major operational and socio-economic challenges in modern metropolises. While surveillance camera networks offer accessible, high-coverage visual feeds, transforming raw video streams into reliable network-wide traffic forecasts requires solving two coupled sub-problems: (1) camera-level visual vehicle count estimation, and (2) network-wide multi-horizon traffic flow forecasting. In this paper, we present a unified framework integrating visual perception and spatio-temporal graph modeling over a real-world urban road network in Ho Chi Minh City, Vietnam. For Sub-problem 1, a ConvNeXt-Tiny backbone with a direct regression head estimates continuous counts of cars and motorbikes directly from camera feeds (trained on 4,000 images, validated on 1,000, and tested on 1,000 images). For Sub-problem 2, we construct a 608-node directed traffic graph $\mathcal{G}=(\mathcal{V}, \mathcal{E}, W)$ built from Ho Chi Minh City Traffic Portal cameras with RBF-weighted road distances. Addressing the fundamental flaws of single-node independent forecasting—which ignores dynamic traffic spillover—we introduce \emph{TA-STGCN} (Temporal Attention-Guided Spatio-Temporal Graph Convolutional Network), an architecture coupling Chebyshev Spectral Graph Convolutions ($K=3$), 1D Gated Temporal Convolutions (GLU), and Model-Level Multi-Head Temporal Self-Attention. Comprehensive evaluations across 5 independent random seeds ($42, 100, 2024, 777, 999$) demonstrate that TA-STGCN achieves superior multi-step forecasting accuracy ($\text{MAE} = 3.1923 \pm 0.0112$, $\text{RMSE} = 4.3435 \pm 0.0142$) across 6 forecast horizons ($t+1$ to $t+6$, 5-to-30 minutes ahead), outperforming traditional GCN-LSTM and standard STGCN baselines while maintaining a significantly lower parameter footprint (165,318 parameters vs. 364,198 for GCN-LSTM and 303,846 for STGCN).
\end{abstract}

\begin{IEEEkeywords}
Intelligent Transportation Systems, Urban Traffic Forecasting, ConvNeXt-Tiny Direct Regression, TA-STGCN, Temporal Attention, Spatio-Temporal Graph Neural Networks.
\end{IEEEkeywords}

% =====================================================================
% SECTION I: INTRODUCTION
% (Old I.A kept; old I.B moved to Section II; old I.C compressed to one
% paragraph, graph definition moved to Sec. III-C; old I.D reduced to a
% short proposal paragraph; old I.E contributions kept with numeric
% results removed from the final bullet.)
% =====================================================================
\section{Introduction}

Rapid global urbanization and the exponential growth of personal motor vehicles—particularly passenger cars and motorcycles—have led to severe urban traffic congestion in developing metropolises, such as Ho Chi Minh City, Vietnam. Constructing new physical road infrastructure requires substantial financial capital and extended timeframes, offering minimal immediate relief. Consequently, Intelligent Transportation Systems (ITS) powered by Artificial Intelligence (AI) have become an essential, cost-effective strategy for real-time traffic perception, adaptive signal control, and proactive congestion forecasting.

Figure~\ref{fig:traffic_jam} illustrates a representative urban traffic congestion scene in Ho Chi Minh City, highlighting extreme vehicle density and complex occlusions between passenger cars and motorcycles.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig/traffic_jam.jpg}
\caption{Urban traffic congestion in Ho Chi Minh City, Vietnam, illustrating extreme vehicle density and severe occlusions between passenger cars and motorcycles.}
\label{fig:traffic_jam}
\end{figure}

In modern urban environments, surveillance camera networks connected to public traffic portals offer high-coverage visual feeds at a fraction of the cost of physical induction loop sensors. However, transforming raw visual camera feeds into proactive city-wide traffic intelligence presents a complex multi-faceted challenge. To address this effectively, the overall urban traffic management framework is decomposed into two sequential sub-problems:
\begin{enumerate}
    \item \textbf{Sub-problem 1 (Camera-Level Perception):} Estimating real-time, category-specific vehicle counts (passenger cars and motorbikes) directly from individual camera feeds under complex outdoor conditions.
    \item \textbf{Sub-problem 2 (Network-Wide Forecasting):} Forecasting future traffic flow trends across the entire interconnected urban road network over multi-step time horizons ($t+1$ to $t+6$, corresponding to 5 to 30 minutes into the future).
\end{enumerate}

A critical methodological insight of this study is that \textbf{single-node independent forecasting is fundamentally inadequate for urban traffic networks}. If each camera location is treated as an isolated univariate time series and predicted independently (e.g., fitting a separate model per road segment), the forecasting system completely ignores spatial topological connectivity. Urban traffic flow is non-stationary and dynamic: congestion at one camera location rapidly spills over and propagates to connected downstream road segments via physical road connections and traffic bottlenecks \cite{ctm}. Predicting traffic at a single node without accounting for spatial graph topology leads to severe error propagation, particularly during peak hours or sudden traffic incidents. Effective traffic forecasting therefore requires modeling the entire camera network as a directed weighted spatial graph (formally defined in Section~\ref{sec:graph_tastgcn}).

To address Sub-problem 2 and overcome the receptive field limitations of prior graph architectures \cite{stgcn, astgcn, stsgcn}, we propose \textbf{TA-STGCN} (Temporal Attention-Guided Spatio-Temporal Graph Convolutional Network), which integrates Chebyshev Spectral Graph Convolutions ($K=3$) \cite{chebnet}, 1D Temporal Gated Convolutions (GLU), and a \emph{Model-Level Multi-Head Temporal Self-Attention} module ($h=4$ heads) \cite{attention}. By applying multi-head temporal self-attention post-ST-Conv blocks, TA-STGCN functions as a dynamic low-pass filter that effectively down-weights high-frequency non-stationary temporal noise while modeling multi-aspect temporal dependencies (immediate momentum, hourly periodicity, and daily patterns) across historical time windows ($T_{\text{in}} = 24$ steps = 120 minutes).

% TODO-R2: Contributions tone/claims to be revisited (avoid "rigorous"/"superior"
% pending significance testing; consider reframing headline as "competitive
% accuracy with 45-55% fewer parameters").
The main contributions of this paper are summarized as follows:
\begin{itemize}
    \item \textbf{Camera-Sensed Framework for Mixed Traffic:} We design a unified two-stage ITS framework that bridges camera-level visual perception (Sub-problem 1) with network-wide spatio-temporal graph forecasting (Sub-problem 2), evaluated end-to-end under motorcycle-dominant mixed traffic conditions where no loop-detector infrastructure exists.
    \item \textbf{Annotated Camera Vehicle Image Dataset:} We contribute a real-world dataset of 6,000 manually annotated traffic camera images (4,000 Train, 1,000 Validation, 1,000 Test) collected across major intersections in Ho Chi Minh City, providing precise continuous count annotations for passenger cars and motorcycles under severe congestion.
    \item \textbf{City-Scale Camera-Only Traffic Graph:} We construct a real-world 608-node spatio-temporal traffic graph harvested via automated multi-threaded API pipelines from 657 CCTV cameras on the Ho Chi Minh City Traffic Portal, paired with topological road distances and RBF-weighted adjacency matrices. To our knowledge, this is among the first network-wide forecasting testbeds built entirely from public camera infrastructure in a motorcycle-dominant metropolis.
% TODO-R2: re-verify "among the first" against a fresh literature pass before
% submission; keep this claim consistent with the identical claim in Sec. II.
    \item \textbf{TA-STGCN Architecture:} We design \emph{TA-STGCN}, introducing Model-Level Multi-Head Temporal Self-Attention \cite{attention} post-ST-Conv blocks to capture long-range temporal dependencies and smooth optimization trajectories without parameter inflation.
    \item \textbf{Multi-Seed Multi-Horizon Benchmark:} We conduct comprehensive multi-seed evaluations across 5 independent random seeds ($42, 100, 2024, 777, 999$) evaluating $H=6$ forecast horizons ($t+1$ to $t+6$). TA-STGCN outperforms the evaluated GCN-LSTM and STGCN baselines across all horizons while requiring approximately 45--55\% fewer parameters.
\end{itemize}

The remainder of this paper is organized as follows. Section~\ref{sec:related} reviews related work. Section~\ref{sec:methods} describes the datasets, the two-stage methodology, and the experimental configuration. Section~\ref{sec:results} presents and discusses the experimental results. Section~\ref{sec:conclusion} concludes the paper.

% =====================================================================
% SECTION II: RELATED WORK
% (Old I.B.1 and I.B.2 moved here verbatim; new positioning paragraph
% added at the end of the section.)
% =====================================================================
\section{Related Work}
\label{sec:related}

Building city-scale traffic intelligence from camera networks requires progress on two coupled fronts: extracting reliable, category-specific vehicle measurements from raw visual feeds, and forecasting how those measurements evolve across a spatially connected road network. The literature on each front is mature, but---as this section shows---it has developed almost entirely under conditions that differ sharply from the camera-sensed, motorcycle-dominant urban traffic this study targets. We review visual vehicle counting and spatio-temporal forecasting in turn, then summarize the resulting gap in Table~\ref{tab:related_comparison}.

\subsection{Visual Vehicle Count Estimation}
Visual vehicle count estimation from surveillance cameras has evolved through three main computational paradigms:
\begin{itemize}
    \item \textbf{Object Detection (e.g., YOLO Series \cite{yolo}):} Detectors draw bounding boxes around individual vehicles per frame. While highly accurate in structured, low-density scenarios, bounding-box regression degrades severely under the high vehicle density, severe occlusions, and chaotic positioning characteristic of mixed traffic. Furthermore, running multi-object detection frame-by-frame incurs high computational overhead, rendering it difficult to deploy across hundreds of city-wide cameras.
    \item \textbf{Density Map Estimation (e.g., MCNN \cite{mcnn}, CSRNet \cite{csrnet}, CAN \cite{can}):} Fully Convolutional Networks map images to pixel-wise density maps, which are then integrated to yield a total vehicle count. Although highly robust to crowd-level occlusions, density mapping inherently struggles to distinguish between distinct vehicle classes (e.g., separating motorcycles from passenger cars) and requires labor-intensive, pixel-level point annotations for training.
    \item \textbf{Direct Regression via Transfer Learning:} Direct regression maps camera images directly to a continuous count vector per class $[\hat{c}_{\text{car}}, \hat{c}_{\text{bike}}]^T$. By utilizing a robust feature backbone like ConvNeXt \cite{convnext} pre-trained on large-scale datasets, direct regression bypasses both the localization bottlenecks of object detection and the class-agnostic limitations of density mapping. This provides a highly stable, computationally efficient solution under dense occlusions while retaining essential category breakdowns.
\end{itemize}

Nearly all existing vehicle-counting benchmarks feature structured, car-dominant, and lane-disciplined scenes. However, mixed traffic---such as the motorcycle-dominant environments typical of Southeast Asian metropolises like Ho Chi Minh City---presents an entirely different challenge. Motorcycles are physically small, cluster densely at intersections, weave unpredictably between larger vehicles without lane discipline, and create severe mutual occlusions. Under these chaotic conditions, standard object detection recall collapses, and class-agnostic density maps discard the very category information (car vs.\ motorbike ratio) that traffic management systems require. This fundamental gap motivates the category-aware direct regression approach adopted in the first stage of our framework.
\midrule
\begin{table*}[ht]
\centering
\caption{Comparative overview of representative spatio-temporal traffic forecasting studies, highlighting the gap in mixed-traffic research.}
\label{tab:related_comparison}
\small
\begin{tabular}{llllc}
\toprule
\textbf{Study} & \textbf{Sensing Modality} & \textbf{Traffic Type} & \textbf{Graph Scale} & \textbf{Category-Aware} \\
\midrule
DCRNN \cite{dcrnn} & Loop detector & Car-dominant, highway & 207 / 325 nodes & No \\
STGCN \cite{stgcn} & Loop detector & Car-dominant, highway & 207 / 325 nodes & No \\
ASTGCN \cite{astgcn} & Loop detector & Car-dominant, highway & $\sim$307 nodes & No \\
Graph WaveNet \cite{wavenet} & Loop detector & Car-dominant, highway & 207 / 325 nodes & No \\
GMAN \cite{gman} & Loop detector & Car-dominant, highway & 207 / 325 nodes & No \\
AGCRN \cite{agcrn} & Loop detector & Car-dominant, highway & $\sim$307 / 170 nodes & No \\
STSGCN \cite{stsgcn} & Loop detector & Car-dominant, highway & $\sim$307 / 170 nodes & No \\
PSO/GA-LSTM \cite{vnlstm} & GPS probe & Car-only, single corridor & N/A (16 segments) & No \\
\midrule
\textbf{TA-STGCN (Ours)} & \textbf{Camera network} & \textbf{Mixed: Car + Motorbike dominant} & \textbf{608 nodes (city-scale)} & \textbf{Yes} \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Spatio-Temporal Traffic Flow Forecasting}
Early traffic forecasting relied on univariate statistical models (e.g., ARIMA \cite{arima}) or classical machine learning algorithms (e.g., Support Vector Regression SVR \cite{svr}). The paradigm has since shifted to deep spatio-temporal graph neural networks \cite{gnn_survey}, which jointly model spatial topology and temporal dynamics:
\begin{itemize}
    \item \textbf{Sequential RNN Baselines (e.g., DCRNN \cite{dcrnn}, T-GCN \cite{tgcn}):} These early models integrate static Graph Convolutional Networks (GCN) \cite{gcn} with Recurrent Neural Networks (LSTM/GRU). While effective at capturing local temporal transitions, sequential RNN architectures suffer from vanishing gradients over long horizons, slow backpropagation, and an inability to parallelize across historical time windows.
    \item \textbf{Convolutional STGNNs (e.g., STGCN \cite{stgcn}, Graph WaveNet \cite{wavenet}):} To overcome RNN bottlenecks, Yu et al. \cite{stgcn} introduced STGCN, replacing recurrent units with parallel 1D Gated Temporal Convolutions (GLU) interleaved with Chebyshev Spectral Graph Convolutions (ChebNet) \cite{chebnet}. Graph WaveNet \cite{wavenet} extended this by combining dilated 1D convolutions with an adaptive dependency matrix. While highly efficient, local 1D temporal convolutions possess restricted receptive fields, limiting their capacity to capture non-stationary, long-range global temporal dependencies.
    \item \textbf{Attention-Based and Adaptive STGNNs (e.g., ASTGCN \cite{astgcn}, GMAN \cite{gman}, AGCRN \cite{agcrn}):} To capture dynamic spatio-temporal correlations, recent architectures heavily utilize attention mechanisms. ASTGCN \cite{astgcn} employs spatial and temporal attention mechanisms, while GMAN \cite{gman} utilizes purely multi-head attention blocks for both domains. While these models often achieve state-of-the-art accuracy on long horizons, their complex attention modules typically incur a massive parameter footprint and quadratic computational complexity, making them difficult to deploy in resource-constrained environments.
\end{itemize}

\subsection{The Key Challenge: Mixed, Motorcycle-Dominant Traffic}
\label{sec:gap}

A critical limitation of the prevailing literature is that almost all established architectures have been developed, tuned, and evaluated on a narrow set of benchmark datasets (e.g., METR-LA, PEMS-BAY, PeMSD4/8) \cite{dcrnn, stgcn, astgcn}. These datasets share fundamental properties that starkly contrast with complex urban realities in developing nations:
\begin{enumerate}
    \item \textbf{Traffic Homogeneity:} They represent car-dominant, lane-disciplined highway traffic where motorcycles play a negligible role.
    \item \textbf{Sensing Infrastructure:} They rely exclusively on highly calibrated, direct electromagnetic induction loop detectors.
    \item \textbf{Predictable Dynamics:} Highway traffic flow largely obeys classical fluid-dynamic traffic models, making propagation patterns relatively stable.
\end{enumerate}

In stark contrast, urban traffic in Southeast Asian metropolises like Ho Chi Minh City is characterized by \textbf{chaotic, motorcycle-dominant mixed traffic} navigating highly unstructured intersections. Motorcycles do not adhere to designated lanes, frequently weave between larger cars and buses, and generate severe mutual occlusions. This physical reality fundamentally alters congestion dynamics: when vehicles cluster densely without lane discipline, standard assumptions of uniform traffic flow and predictable velocity completely break down. 

Because of these chaotic movement patterns, mixed-traffic environments exhibit highly non-stationary temporal fluctuations and sudden spatial spillovers that standard highway-trained architectures struggle to capture. Existing work targeting mixed traffic remains exceedingly sparse. For instance, a recent study \cite{vnlstm} applies optimization-enhanced LSTMs to GPS probe data from commercial vehicles in Vietnam; however, it ignores network-wide spatial spillover and completely misses the vast motorcycle majority of the traffic stream since motorcycles lack GPS probes.

Table~\ref{tab:related_comparison} summarizes these critical distinctions. Most notably, no existing benchmark addresses the combined challenge of camera-only sensing and chaotic, non-lane-disciplined mixed traffic at a network-wide scale.


% TODO-R2: verify node counts and parameter regimes for baseline studies
% against primary sources before submission (the table currently reports
% commonly cited literature configurations, not independently re-verified
% figures). Note "Category-Aware" reflects Stage 1 perception output only;
% Stage 2 forecasting in the present version aggregates to total volume
% (see Sec. IV-D Limitations) -- update this cell if per-class forecasting
% is adopted.

As Table~\ref{tab:related_comparison} makes explicit, none of the reviewed forecasting architectures has been evaluated under camera-only sensing or motorcycle-dominant mixed traffic, and none preserves per-category vehicle information beyond the perception stage. This study is positioned directly at that intersection: it couples category-aware direct-regression perception with network-wide graph forecasting, evaluated end-to-end on a real, city-scale 608-node camera network in Ho Chi Minh City---to our knowledge among the first such testbeds for a motorcycle-dominant metropolis---using a temporal-attention architecture whose parameter economy suits the modest computational budgets realistic for such deployments.
% TODO-R2: (a) re-verify the "among the first" claim against a fresh literature
% pass before submission; (b) if per-class (car/motorbike) forecasting is
% adopted in Stage 2, strengthen this closing claim accordingly.
%=====================================================================
% SECTION III: MATERIALS AND METHODS
% III-A Datasets and Study Area          <- old II-B.1 (camera harvesting)
%                                           + old IV-A.1 + old IV-A.2
% III-B Camera-Level Vehicle Count Est.  <- old II-A
% III-C Graph Construction and TA-STGCN  <- old II-B.2..8 (+ graph def from old I.C)
% III-D Baselines and Experimental Config<- old III (deduplicated) + old IV-B + IV-C
% =====================================================================
\section{Materials and Methods}
\label{sec:methods}

This section describes the study area and datasets, followed by the mathematical formulations and system components for both sub-problems: Sub-problem 1 (Camera-Level Vehicle Count Estimation via ConvNeXt-Tiny \cite{convnext} vs. ResNet-50 \cite{resnet}) and Sub-problem 2 (Network-Wide Spatio-Temporal Graph Forecasting via TA-STGCN \cite{stgcn, attention}).

\subsection{Datasets and Study Area}
\label{sec:datasets}

\subsubsection{Camera Network Acquisition}
We harvest surveillance camera feeds from the Ho Chi Minh City Traffic Portal. Initially, 657 camera feeds are acquired along with Camera IDs, location names, and spatial coordinates (Latitude, Longitude). After filtering out faulty, offline, or misaligned feeds, \textbf{608 active camera stations} are established as the graph node set $\mathcal{V}$ ($N = |\mathcal{V}| = 608$).

An automated multi-threaded API downloader with HTTP session pooling scans all 608 cameras every 60 seconds, building a synchronized, time-aligned sequence of traffic images. Figure~\ref{fig:api_samples} shows representative traffic camera feeds collected from the Ho Chi Minh City Traffic Portal API across major urban intersections.
% TODO-R2: state the data collection period (start/end dates, number of days)
% -- required to interpret the chronological 80/10/10 split.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig/samples_pictures_get_from_api.png}
\caption{Sample surveillance camera feeds captured from the Ho Chi Minh City Traffic Portal across diverse urban intersections.}
\label{fig:api_samples}
\end{figure}

\subsubsection{Sub-Problem 1 Vision Dataset}
The camera vehicle estimation dataset consists of \textbf{6,000 manually annotated traffic images} captured across surveillance cameras from the Ho Chi Minh City Traffic Portal. The dataset is partitioned into:
\begin{itemize}
    \item \textbf{Training Set:} 4,000 images.
    \item \textbf{Validation Set:} 1,000 images.
    \item \textbf{Testing Set:} 1,000 images.
\end{itemize}
Ground-truth annotations provide individual continuous counts for passenger cars and motorbikes. Images are resized to $224 \times 224$ pixels and normalized with ImageNet mean and standard deviation.
% TODO-R2: state how many distinct cameras contribute to the 6,000 images and
% whether train/val/test splits are disjoint by camera (camera-level leakage check).

\subsubsection{Sub-Problem 2 Traffic Graph Time-Series Dataset}
Using Stage 1 predictions across 608 active camera nodes, time series are aggregated at 5-minute intervals ($step\_minutes=5$). Missing steps ($\le 30$ minutes) are linearly interpolated and smoothed via double-rolling transformation ($window_1=3$, $window_2=5$).

Historical lookback length is $T_{\text{in}} = 24$ steps (120 minutes history), and forecast horizon is $H = 6$ steps (30 minutes ahead).

The time-series dataset is split chronologically:
\begin{itemize}
    \item \textbf{Training Set:} First 80\% of the time range.
    \item \textbf{Validation Set:} Middle 10\% of the time range.
    \item \textbf{Testing Set:} Final 10\% of the time range.
\end{itemize}
% TODO-R2: clarify that (a) normalization statistics are fit on the training
% partition only, and (b) interpolation/rolling smoothing do not cross split
% boundaries (temporal leakage check). Also note explicitly that Stage 2
% targets are Stage 1 predictions (pseudo-labels), cross-referenced in the
% Limitations discussion (Sec. IV-D).

\subsection{Sub-Problem 1: Camera-Level Vehicle Count Estimation}
\label{sec:subproblem1}

Sub-problem 1 maps individual camera images to continuous vehicle counts per category $[\hat{c}_{\text{car}}, \hat{c}_{\text{bike}}]^T$. We compare two deep convolutional feature backbones: the baseline ResNet-50 \cite{resnet} and the proposed ConvNeXt-Tiny \cite{convnext}.

\subsubsection{ResNet-50 Baseline Backbone}
ResNet-50 \cite{resnet} is utilized as a standard baseline. We project the input image $I \in \mathbb{R}^{3 \times 224 \times 224}$ into a 2048-dimensional feature representation via Global Average Pooling (GAP):
\begin{equation}
\mathbf{f}_{\text{resnet}} = \text{GAP}\left(\mathcal{F}_{\text{ResNet50}}(I)\right) \in \mathbb{R}^{2048}.
\end{equation}

\subsubsection{ConvNeXt-Tiny Feature Backbone (Proposed)}
For robust visual feature extraction, we adopt the standard ConvNeXt-Tiny architecture \cite{convnext}. Retaining the original unmodified backbone configuration, we extract a compact, high-level visual representation $\mathbf{f}_{\text{visual}} \in \mathbb{R}^{768}$ via Global Average Pooling:
\begin{equation}
\mathbf{f}_{\text{visual}} = \text{GAP}\left(\mathcal{F}_{\text{ConvNeXt}}(I)\right) \in \mathbb{R}^{768}.
\end{equation}

Figure~\ref{fig:convnext_counting_arch} illustrates the pipeline of the proposed ConvNeXt-Tiny vehicle count estimation model, showcasing the original backbone followed by the custom MLP regression head.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/ConvNext_tiny_coutin.png}
\caption{Architectural pipeline of the proposed ConvNeXt-Tiny vehicle count estimation model, utilizing the standard backbone and a custom MLP regression head for continuous car and motorbike count estimation.}
\label{fig:convnext_counting_arch}
\end{figure*}

\subsubsection{Custom MLP Regression Head}
For both backbones, the ImageNet classification layer is removed and replaced by a custom Multi-Layer Perceptron (MLP) regression head:
\begin{equation}
\mathbf{h}_1 = \text{LayerNorm}\left( \mathbf{W}_1 \mathbf{f}_{\text{visual}} + \mathbf{b}_1 \right), \quad \mathbf{W}_1 \in \mathbb{R}^{128 \times 768},
\end{equation}
\begin{equation}
\hat{\mathbf{c}} = \mathbf{W}_2 \cdot \text{ReLU}(\mathbf{h}_1) + \mathbf{b}_2, \quad \mathbf{W}_2 \in \mathbb{R}^{2 \times 128},
\end{equation}
where $\hat{\mathbf{c}} = [\hat{c}_{\text{car}}, \hat{c}_{\text{bike}}]^T \in \mathbb{R}^2$ represents the estimated continuous counts of passenger cars and motorbikes in image $I$.

\subsubsection{Objective Function}
Sub-problem 1 is trained end-to-end using the Smooth $L_1$ (Huber) Loss \cite{huber} to provide robustness against annotation noise in dense traffic scenes:
\begin{equation}
\mathcal{L}_{\text{vision}}(\mathbf{c}, \hat{\mathbf{c}}) = \frac{1}{2} \sum_{m \in \{\text{car}, \text{bike}\}} \text{Smooth}_{L_1}\left(c_m - \hat{c}_m\right),
\end{equation}
where
\begin{equation}
\text{Smooth}_{L_1}(x) = \begin{cases} 0.5 x^2, & \text{if } |x| < 1 \\ |x| - 0.5, & \text{otherwise.} \end{cases}
\end{equation}

\subsection{Sub-Problem 2: Traffic Graph Construction and TA-STGCN}
\label{sec:graph_tastgcn}

To capture spatial traffic spillover across the city, estimated vehicle counts across camera locations are integrated into an urban road network graph. The entire camera network is modeled as a directed weighted spatial graph $\mathcal{G} = (\mathcal{V}, \mathcal{E}, W)$, where $\mathcal{V}$ represents the set of camera nodes ($N=608$ active camera stations across Ho Chi Minh City, Section~\ref{sec:datasets}), $\mathcal{E}$ represents directed road connections, and $W$ is a Radial Basis Function (RBF) weighted distance matrix.

\subsubsection{Graph Edges and RBF Adjacency Matrix}
Graph edges $\mathcal{E}$ represent physical road connections between adjacent camera stations. Edge weights $W_{ij} \in [0, 1]$ are derived from topological road travel distances $d_{ij}$ using a Radial Basis Function (RBF) kernel:
\begin{equation}
W_{ij} = \begin{cases} \exp\left(-\frac{d_{ij}^2}{\sigma^2}\right), & \text{if } i \neq j \text{ and } d_{ij} \le \tau \\ 0, & \text{otherwise,} \end{cases}
\end{equation}
where $\sigma$ is the standard deviation of road distances and $\tau$ is the spatial distance cutoff threshold.
% TODO-R2: report concrete values of sigma and tau, and the source of the road
% travel distances d_ij (OSM routing / API). Clarify how directedness of E is
% obtained given the thresholded-RBF construction.

The normalized Graph Laplacian $L$ and Scaled Laplacian $\tilde{L}$ are computed as:
\begin{equation}
L = I_N - D^{-1/2} W D^{-1/2}, \quad \tilde{L} = \frac{2}{\lambda_{\max}} L - I_N,
\end{equation}
where $D_{ii} = \sum_{j} W_{ij}$ is the diagonal degree matrix and $\lambda_{\max}$ is the maximum eigenvalue of $L$.

\subsubsection{Historical Feature Tensor and Target Horizon}
Outputs from Sub-problem 1 are aggregated at 5-minute intervals. Each node $i$ contains a 4-dimensional feature vector $\mathbf{x}_{i,t} \in \mathbb{R}^4$:
\begin{enumerate}
    \item Normalized total vehicle volume $v_{i,t}$.
    \item Sine time-of-day feature: $\sin\left(\frac{2\pi \cdot \text{tod}}{1440}\right)$.
    \item Cosine time-of-day feature: $\cos\left(\frac{2\pi \cdot \text{tod}}{1440}\right)$.
    \item Normalized hour of day: $\frac{\text{hour}}{24}$.
\end{enumerate}

Given historical time series over $T_{\text{in}} = 24$ steps (120 minutes of history), the historical input tensor is $X \in \mathbb{R}^{B \times T_{\text{in}} \times N \times F}$ ($F=4$). The target is forecasting future traffic volume across horizon $H = 6$ steps (30 minutes ahead): $Y \in \mathbb{R}^{B \times H \times N \times 1}$.

TA-STGCN couples Chebyshev Spectral Graph Convolutions \cite{chebnet}, 1D Gated Temporal Convolutions (GLU) \cite{stgcn}, and Model-Level Multi-Head Temporal Self-Attention \cite{attention}. Figure~\ref{fig:stgcn_architecture} illustrates the structural diagram of the standard STGCN baseline architecture \cite{stgcn}.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig/stgcn_architecture.png}
\caption{Architectural overview of the standard STGCN baseline model \cite{stgcn}, combining Chebyshev Spectral Graph Convolutions and 1D Temporal GLU blocks. Our proposed TA-STGCN augments this framework with a Model-Level Multi-Head Temporal Self-Attention module post-ST-Conv blocks.}
\label{fig:stgcn_architecture}
\end{figure}

\subsubsection{1D Temporal Gated Convolution (GLU)}
For feature map $H \in \mathbb{R}^{B \times C_{\text{in}} \times N \times T}$, 1D causal convolution doubles channels to $2 C_{\text{out}}$ \cite{stgcn}:
\begin{equation}
\text{Conv}_{1D}(H) = [P, Q] \in \mathbb{R}^{B \times 2 C_{\text{out}} \times N \times T'},
\end{equation}
\begin{equation}
\text{GLU}(H) = P \odot \sigma(Q) \in \mathbb{R}^{B \times C_{\text{out}} \times N \times T'},
\end{equation}
where $\odot$ denotes element-wise product and $\sigma(\cdot)$ is the sigmoid gate.

\subsubsection{Chebyshev Spectral Graph Convolution (ChebNet)}
Spatial graph convolution is approximated using truncated Chebyshev polynomials up to order $K=3$ \cite{chebnet}:
\begin{equation}
g_{\theta} \star_{\mathcal{G}} x \approx \sum_{k=0}^{K-1} \mathbf{\Theta}_k T_k(\tilde{L}) x,
\end{equation}
where $T_0(\tilde{L}) = I_N$, $T_1(\tilde{L}) = \tilde{L}$, and $T_k(\tilde{L}) = 2 \tilde{L} T_{k-1}(\tilde{L}) - T_{k-2}(\tilde{L})$.

\subsubsection{ST-Conv Block Assembly}
Each $ST\text{-Conv}$ block sandwiches spatial ChebNet graph convolution between two temporal GLUs with residual connection and LayerNorm \cite{stgcn}:
\begin{equation}
H^{(1)} = \text{GLU}_1(X), \quad H^{(2)} = \text{ReLU}\left( \text{ChebConv}\left(H^{(1)}, \tilde{L}\right) \right),
\end{equation}
\begin{equation}
H^{(3)} = \text{GLU}_2\left(H^{(2)}\right),
\end{equation}
\begin{equation}
H_{\text{block}} = \text{Dropout}\left( \text{LayerNorm}\left( H^{(3)} + \text{Residual}(X) \right) \right).
\end{equation}

\subsubsection{Model-Level Multi-Head Temporal Self-Attention}
To capture dynamic long-range temporal dependencies across the 24-step historical window, TA-STGCN applies \textbf{Multi-Head Temporal Self-Attention} \cite{attention} at the model output stage (post ST-Conv blocks).

The feature tensor $H_{\text{stgcn}} \in \mathbb{R}^{B \times C \times N \times T}$ is permuted and reshaped into node sequence batches:
\begin{equation}
Z = \text{Reshape}\left(\text{Permute}(H_{\text{stgcn}})\right) \in \mathbb{R}^{(B \cdot N) \times T \times C}.
\end{equation}

For $h=4$ attention heads \cite{attention}, Query, Key, and Value matrices are projected:
\begin{equation}
Q_m = Z W_m^Q, \quad K_m = Z W_m^K, \quad V_m = Z W_m^V, \quad m \in \{1, \dots, h\}.
\end{equation}
Scaled dot-product attention per head is computed as:
\begin{equation}
\text{head}_m = \text{Softmax}\left( \frac{Q_m K_m^T}{\sqrt{d_k}} \right) V_m, \quad d_k = C / h.
\end{equation}
\begin{equation}
\text{MHA}(Z) = \text{Concat}(\text{head}_1, \dots, \text{head}_h) W^O.
\end{equation}

A Transformer-style block \cite{attention} with residual connection, LayerNorm, and GELU Feed-Forward Network (FFN) updates temporal representations:
\begin{equation}
Z^{(1)} = \text{LayerNorm}\left( Z + \text{MHA}(Z) \right),
\end{equation}
\begin{equation}
Z^{(2)} = \text{LayerNorm}\left( Z^{(1)} + \text{FFN}(Z^{(1)}) \right),
\end{equation}
where $\text{FFN}(u) = \text{Linear}_{2}\left( \text{Dropout}\left( \text{GELU}\left( \text{Linear}_{1}(u) \right) \right) \right)$ with 4x dimension expansion ($C \to 4C \to C$).

\subsubsection{Output Conv1D Time-Reduction \& Objective Function}
The attention output tensor $Z^{(2)}$ is permuted back to $\mathbb{R}^{(B \cdot N) \times C \times T}$ and passed to a 1D output convolution with kernel size $T_{\text{in}}$ to collapse time dimension $T_{\text{in}} \to 1$:
\begin{equation}
\hat{Y} = \text{Reshape}\left( \text{Conv1D}_{T_{\text{in}} \to 1}\left(Z^{(2)}\right) \right) \in \mathbb{R}^{B \times H \times N \times 1}.
\end{equation}

The network is trained end-to-end using Pure Huber Loss \cite{huber} ($\delta = 1.0$):
\begin{equation}
\mathcal{L}_{\text{GNN}}(Y, \hat{Y}) = \begin{cases} \frac{1}{2}(Y - \hat{Y})^2, & \text{if } |Y - \hat{Y}| \le \delta \\ \delta |Y - \hat{Y}| - \frac{1}{2}\delta^2, & \text{otherwise.} \end{cases}
\end{equation}

\subsection{Baseline Architectures and Experimental Configuration}
\label{sec:baselines_config}

To establish a rigorous benchmark for Sub-problem 2, we evaluate two foundational baseline architectures against our proposed TA-STGCN model \cite{stgcn, attention}. Both baseline models are intentionally configured with higher parameter capacity to prove that TA-STGCN's performance superiority originates from structural architectural innovation rather than parameter scaling.

\subsubsection{Evaluated Architectures}
\textbf{Baseline 1 (GCN-LSTM)} represents classical recurrent Spatio-Temporal Graph Neural Networks (STGNNs) widely adopted as standard baselines in urban traffic forecasting—such as T-GCN (Zhao et al., \emph{IEEE T-ITS} 2019) \cite{tgcn}, GC-LSTM (Lu et al., \emph{IEEE Access} 2020) \cite{gclstm}, and DCRNN (Li et al., \emph{ICLR} 2018) \cite{dcrnn}. It combines a 2-layer static Graph Convolutional Network (GCN) \cite{gcn} for spatial topological feature extraction (utilizing the normalized symmetric graph Laplacian $A_{\text{norm}} = D^{-1/2} (A + I_N) D^{-1/2}$) with a 2-layer Long Short-Term Memory (LSTM) network for sequential temporal modeling, followed by a shared MLP output projection ($160 \to 80 \to H$). \textbf{Baseline 2 (Standard STGCN)} is the classic architecture introduced by Yu et al. \cite{stgcn} (IJCAI 2018), utilizing convolutional blocks without attention mechanisms. \textbf{TA-STGCN (Proposed)} couples 2 ST-Conv blocks \cite{stgcn} with the Model-Level Multi-Head Temporal Self-Attention module described in Section~\ref{sec:graph_tastgcn}.

To enable a fair and transparent comparison across different learning paradigms, Table~\ref{tab:model_comparison} summarizes the structural components, feature dimensions, and parameter allocations for all evaluated models in Sub-problem 2.

\begin{table*}[t]
\centering
\caption{Architectural and Parameter Comparison of Evaluated Models for Sub-Problem 2 (Network-Wide Forecasting).}
\label{tab:model_comparison}
\resizebox{\textwidth}{!}{%
\setlength{\tabcolsep}{12pt}
\begin{tabular}{lccc}
\toprule
\textbf{Architectural Attribute} & \textbf{GCN-LSTM (Baseline 1)} & \textbf{STGCN (Baseline 2)} & \textbf{TA-STGCN (Proposed / Ours)} \\
\midrule
\textbf{Trainable Parameter Count} & \textbf{364,198} (Highest Capacity) & \textbf{303,846} (2nd Largest) & \textbf{165,318} (\textbf{Most Lightweight}) \\
\textbf{Spatial Feature Extraction} & Static GCN (2 Layers) \cite{gcn, tgcn} & ChebNet ($K=3$) \cite{chebnet} & ChebNet ($K=3$) \cite{chebnet} \\
\textbf{Temporal Sequence Modeling} & Sequential LSTM (2 Layers) \cite{tgcn, gclstm} & 1D Gated Conv (GLU) \cite{stgcn} & 1D GLU + \textbf{Model-Level Temporal Attention} \cite{attention} \\
\textbf{Attention Heads ($h$)} & 0 (No Attention) & 0 (No Attention) & \textbf{$h=2$ Heads} \cite{attention} \\
\textbf{Number of Blocks} & N/A & 3 Blocks & \textbf{2 Blocks} \\
\textbf{Hidden Channel Dimensions} & GCN = 64, LSTM = 160 & $C = 80$ & $C = 64$ \\
\textbf{Loss Function} & Smooth Huber Loss & Smooth Huber Loss & \textbf{Pure Huber Loss ($\delta=1.0$)} \\
\bottomrule
\end{tabular}%
}
\end{table*}

The architectural choices in Table~\ref{tab:model_comparison} highlight three key computational trade-offs:
\begin{enumerate}
    \item \textbf{Spatial Convolution Operators:} Baseline 1 relies on 1st-order static GCN convolutions \cite{gcn, tgcn, gclstm}, which only aggregate immediate 1-hop spatial neighborhoods. In contrast, both Baseline 2 \cite{stgcn} and TA-STGCN utilize 3rd-order Chebyshev Spectral Polynomials ($K=3$) \cite{chebnet}, allowing the networks to capture multi-hop spatial dependencies across localized road neighborhoods without requiring dense matrix exponentiation.
    \item \textbf{Temporal Sequence Modeling Mechanisms:} Baseline 1 adopts sequential LSTM cells \cite{tgcn, gclstm}, which process historical time steps step-by-step ($t=1 \dots T_{\text{in}}$). While LSTMs effectively capture local temporal transitions, their sequential recurrence prevents parallel GPU acceleration and suffers from gradient attenuation over long horizons. Baseline 2 \cite{stgcn} resolves training latency by replacing RNNs with parallel 1D Gated Linear Units (GLU). However, local 1D convolutions possess fixed temporal receptive fields ($K_t=3$), restricting their ability to model non-stationary, long-range temporal correlations across the 24-step historical window. TA-STGCN bridges this gap by augmenting ST-Conv blocks with a Model-Level Multi-Head Temporal Self-Attention module ($h=2$) \cite{attention}, dynamically weighing temporal correlations across the entire 120-minute history.
    \item \textbf{Controlled Capacity Design:} To eliminate parameter scaling bias, Baseline 1 and Baseline 2 are intentionally allocated larger parameter budgets than TA-STGCN (a 54.6\% and 45.6\% parameter reduction, respectively; see Table~\ref{tab:model_comparison}), so that any accuracy gains can be attributed to structural temporal attention gating \cite{attention} rather than brute-force capacity expansion.
\end{enumerate}

\subsubsection{Training Hyperparameters}
All models across both sub-problems are implemented in PyTorch 2.x and trained hardware-accelerated on an \textbf{NVIDIA T4 GPU (16 GB VRAM)}.

The vision backbones (ConvNeXt-Tiny \cite{convnext} and ResNet-50 \cite{resnet}) with custom MLP regression heads are trained under the following setup:
\begin{itemize}
    \item \textbf{Optimizer:} AdamW optimizer with initial learning rate $\eta = 1 \times 10^{-4}$ and weight decay coefficient $1 \times 10^{-2}$.
    \item \textbf{Learning Rate Schedule:} Cosine Annealing Learning Rate Scheduler decaying down to $\eta_{\min} = 1 \times 10^{-6}$.
    \item \textbf{Epochs and Batch Size:} Models are trained for 100 epochs with a mini-batch size of $B = 32$.
    \item \textbf{Loss Function:} Smooth $L_1$ (Huber) Loss \cite{huber} with threshold $\beta = 1.0$.
    \item \textbf{Data Augmentations:} Random horizontal flips (probability $p=0.5$), color jittering ($\pm 15\%$ brightness, contrast, and saturation), and random spatial cropping resized to $224 \times 224$.
\end{itemize}

The spatio-temporal graph models (GCN-LSTM \cite{gcn, tgcn}, STGCN Baseline \cite{stgcn}, and TA-STGCN \cite{attention}) are trained under the following setup:
\begin{itemize}
    \item \textbf{Optimizer:} Adam optimizer \cite{adam} with initial learning rate $\eta = 5 \times 10^{-4}$ and weight decay coefficient $1 \times 10^{-4}$.
    \item \textbf{Learning Rate Schedule:} ReduceLROnPlateau scheduler decaying the learning rate by a factor of $0.5$ if validation loss fails to improve for 10 consecutive epochs ($\text{patience}=10$).
    \item \textbf{Max Epochs and Early Stopping:} Trained for up to 500 epochs with an early stopping patience of 30 epochs based on Validation MAE.
    \item \textbf{Batch Size:} Training mini-batch size $B = 16$ (evaluation batch size $\min(B, 32) = 16$).
    \item \textbf{Loss Function:} Pure Huber Loss \cite{huber} ($\delta = 1.0$) trained end-to-end.
    \item \textbf{Multi-Seed Evaluation Protocol:} Models are evaluated across \textbf{5 independent random seeds}: $\mathcal{S} = \{42, 100, 2024, 777, 999\}$. All reported metrics present $\text{Mean} \pm \text{Std}$ over the 5 seeds.
\end{itemize}
% TODO-R2: document the hyperparameter search performed for the GCN-LSTM
% baseline (its very high MAE suggests undertuning; reviewers will probe this).

\subsubsection{Evaluation Metrics}
% NOTE (Round 1): the separate MSE equation was removed as redundant
% (RMSE = sqrt(MSE)); the MSE row in the benchmark table was removed accordingly.
Model performance is evaluated using Mean Absolute Error (MAE) and Root Mean Squared Error (RMSE):
\begin{equation}
\text{MAE} = \frac{1}{N \cdot H \cdot M} \sum_{i=1}^{N} \sum_{h=1}^{H} \sum_{k=1}^{M} \left| y_{i,h}^{(k)} - \hat{y}_{i,h}^{(k)} \right|,
\end{equation}
\begin{equation}
\text{RMSE} = \sqrt{ \frac{1}{N \cdot H \cdot M} \sum_{i=1}^{N} \sum_{h=1}^{H} \sum_{k=1}^{M} \left( y_{i,h}^{(k)} - \hat{y}_{i,h}^{(k)} \right)^2 },
\end{equation}
where $N=608$ graph nodes, $H=6$ forecast steps, and $M$ is the test batch count.
% TODO-R2: consider adding a normalized metric (e.g., MAPE or MAE relative to
% mean node volume) so that MAE = 3.19 vehicles is interpretable.

% =====================================================================
% SECTION IV: RESULTS AND DISCUSSION
% IV-A <- old V-A + Fig 5 + old VI-A (feature attribution, moved up)
% IV-B <- old V-B (Table III) + old V-C (convergence) + old V-D (node viz)
%         + old VI-B (temporal attention analysis, moved up)
% IV-C <- old V-F ablation (empty variants removed per author decision)
% IV-D <- old V-E comparative discussion (deduplicated) + NEW limitations
% =====================================================================
\section{Results and Discussion}
\label{sec:results}

This section presents empirical evaluation and qualitative analysis for Sub-problem 1 (Camera-Level Vehicle Count Estimation) and Sub-problem 2 (Network-Wide Multi-Horizon Traffic Forecasting).

\subsection{Sub-Problem 1: Vehicle Count Estimation}

Table~\ref{tab:stage1_results} reports the vehicle count regression performance evaluated on the 1,000-image test set from Ho Chi Minh City traffic cameras.

\begin{table}[h]
\centering
\caption{Sub-Problem 1 Vehicle Count Estimation Performance.}
\label{tab:stage1_results}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcc}
\toprule
\textbf{Vision Backbone Architecture} & \textbf{MAE (Vehicles)} & \textbf{MSE (Vehicles)} \\
\midrule
ResNet-50 Baseline \cite{resnet} & 4.03 & 45.17 \\
\textbf{ConvNeXt-Tiny (Proposed)} \cite{convnext} & \textbf{3.53} & \textbf{42.13} \\
\bottomrule
\end{tabular}%
}
\end{table}

\textbf{ConvNeXt-Tiny} achieves superior count regression accuracy, reducing Mean Absolute Error (MAE) to \textbf{3.53 vehicles per frame} (outperforming the ResNet-50 baseline MAE of 4.03). This error margin corresponds to an average deviation of only 2--3 motorbikes and 0--1 passenger cars per camera view. In contrast to object detection frameworks whose bounding-box recall drops sharply under dense traffic jams, ConvNeXt-Tiny's inverted bottleneck stem and $7 \times 7$ depthwise convolutions maintain highly stable estimation across high-density urban scenes.

Figure~\ref{fig:counting_training} illustrates the training and validation MAE/Loss convergence curves for the ConvNeXt-Tiny vehicle counting model over training epochs.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/results_training_model_counting.png}
\caption{Training and validation MAE and Loss convergence curves for Stage 1 ConvNeXt-Tiny vehicle count regression model. \textbf{ẢNH MỜ QUÁ}}
\label{fig:counting_training}
\end{figure*}

% --- Moved from old Section VI-A; terminology corrected from "attention
% heatmaps" to "feature attribution maps" (ConvNeXt has no attention layers). ---
To verify that the ConvNeXt-Tiny vehicle count regression grounds its predictions in physical vehicle visual features rather than background environmental shortcuts (such as road markings, tree shadows, or weather glare), we analyze feature attribution maps. Figure~\ref{fig:attention_map} presents feature attribution maps generated for the ConvNeXt-Tiny vehicle counting model across representative traffic camera scenes.
% TODO-R2: specify the attribution method used (e.g., Grad-CAM) in text and caption.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/attention_map.png}
\caption{Feature attribution maps for the ConvNeXt-Tiny vehicle count regression model, confirming high activation focus on vehicles (motorbikes and cars) in camera feeds.}
\label{fig:attention_map}
\end{figure*}

The feature attribution maps demonstrate that ConvNeXt-Tiny concentrates high activation weights directly on active vehicle clusters (passenger cars and motorcycle streams). The $7 \times 7$ depthwise receptive fields effectively isolate vehicle regions even under partial occlusions and high traffic density, confirming that direct regression learns genuine visual representations of vehicle presence.

\subsection{Sub-Problem 2: Multi-Horizon Traffic Forecasting}

Table~\ref{tab:benchmark_5seeds} presents the multi-horizon forecasting performance averaged across \textbf{5 independent random seeds} ($\text{Mean} \pm \text{Std}$) for seeds $\mathcal{S} = \{42, 100, 2024, 777, 999\}$. Evaluated architectures are compared across all 6 prediction horizons ($t+1$ to $t+6$, corresponding to 5, 10, 15, 20, 25, and 30 minutes ahead) on the 608-node traffic graph.

\begin{table*}[t]
\centering
\caption{Sub-Problem 2 Multi-Horizon Traffic Forecasting Benchmark Across 5 Random Seeds ($\text{Mean} \pm \text{Std}$).}
\label{tab:benchmark_5seeds}
\resizebox{\textwidth}{!}{%
\setlength{\tabcolsep}{10pt}
\begin{tabular}{lccc}
\toprule
\textbf{Evaluation Metric / Forecast Horizon} & \textbf{GCN-LSTM (Baseline 1)} & \textbf{STGCN (Baseline 2)} & \textbf{TA-STGCN (Proposed / Ours)} \\
\midrule
\textbf{Trainable Parameter Count} & 364,198 (Highest Capacity) & 303,846 (2nd Largest) & \textbf{165,318} (\textbf{Most Lightweight}) \\
\midrule
\textbf{MAE Overall} & $8.5240 \pm 0.1200$ & $3.2125 \pm 0.0151$ & $\mathbf{3.1923 \pm 0.0112}$ (\textbf{Best}) \\
\textbf{MAE $t+1$ (5 min ahead)} & $5.1230 \pm 0.0800$ & $2.8590 \pm 0.0211$ & $\mathbf{2.8035 \pm 0.0161}$ (\textbf{Best}) \\
\textbf{MAE $t+2$ (10 min ahead)} & $6.7840 \pm 0.1000$ & $3.0968 \pm 0.0175$ & $\mathbf{3.0660 \pm 0.0110}$ (\textbf{Best}) \\
\textbf{MAE $t+3$ (15 min ahead)} & $8.2150 \pm 0.1100$ & $3.2161 \pm 0.0163$ & $\mathbf{3.1954 \pm 0.0105}$ (\textbf{Best}) \\
\textbf{MAE $t+4$ (20 min ahead)} & $9.4510 \pm 0.1300$ & $3.2976 \pm 0.0141$ & $\mathbf{3.2889 \pm 0.0108}$ (\textbf{Best}) \\
\textbf{MAE $t+5$ (25 min ahead)} & $10.5420 \pm 0.1400$ & $3.3710 \pm 0.0148$ & $\mathbf{3.3669 \pm 0.0123}$ (\textbf{Best}) \\
\textbf{MAE $t+6$ (30 min ahead)} & $11.5120 \pm 0.1600$ & $3.4345 \pm 0.0173$ & $\mathbf{3.4329 \pm 0.0124}$ (\textbf{Best}) \\
\midrule
\textbf{RMSE Overall} & $14.8250 \pm 0.2200$ & $4.3653 \pm 0.0215$ & $\mathbf{4.3435 \pm 0.0142}$ (\textbf{Best}) \\
\bottomrule
\end{tabular}%
}
\end{table*}
% NOTE (Round 1): MSE row removed (redundant with RMSE).
% TODO-R2: add paired significance test (e.g., Wilcoxon signed-rank over seeds)
% for TA-STGCN vs. STGCN before claiming per-horizon superiority ("Best").

To analyze training dynamic behavior and noise resilience, Figure~\ref{fig:compare_mae_curves} plots the validation MAE curves across training epochs among \textbf{GCN-LSTM (Baseline 1)}, \textbf{STGCN (Baseline 2)}, and \textbf{TA-STGCN (Proposed Model)} for a representative random seed.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/fig_compare_mae_curve_models_graph.png}
\caption{Comparison of Validation MAE convergence curves across training epochs among GCN-LSTM (Baseline 1), STGCN (Baseline 2), and TA-STGCN (Proposed Model) for a representative seed. \textbf{ĐỔI ẢNH KHÁC RÕ HƠN, NỀN MÀU TRẮNG}}
\label{fig:compare_mae_curves}
\end{figure*}

The convergence curves in Figure~\ref{fig:compare_mae_curves} reveal fundamental insights into model optimization dynamics:
\begin{enumerate}
    \item \textbf{Inefficiency of Recurrent GCN-LSTM Baseline:} \textbf{GCN-LSTM} exhibits severe convergence stagnation, plateauing at a significantly higher validation MAE ($\sim 8.52$). Because static 2-layer GCN is uncoupled from sequential LSTM cells, spatial topologies and temporal dynamics are processed in isolation. Furthermore, backpropagating through 24 sequential LSTM steps causes severe gradient attenuation, preventing the model from effectively minimizing loss across long forecast horizons.
    \item \textbf{Superiority of Convolutional and Attention STGNNs:} In contrast, both \textbf{STGCN Baseline} and \textbf{TA-STGCN} rapidly converge to much lower error levels ($\sim 3.21$ and $3.19$) due to parallel 1D gated temporal convolutions and Chebyshev spectral graph convolutions.
    \item \textbf{Smooth Dynamic Low-Pass Filtering in TA-STGCN:} \textbf{TA-STGCN produces a significantly smoother, less oscillatory validation MAE curve} than standard STGCN. While standard STGCN's local 1D convolutions ($K_t=3$) suffer from spikes caused by camera noise, TA-STGCN's Model-Level Multi-Head Temporal Self-Attention ($h=4$) \cite{attention} acts as a \emph{dynamic low-pass filter}, down-weighting transient anomalies and ensuring steady, monotonic convergence.
\end{enumerate}

Figure~\ref{fig:node_prediction} illustrates the ground-truth traffic volume curve compared against predictions generated by \textbf{TA-STGCN} over test time steps for a representative camera node in the 608-node graph.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/result_of_a_node_by_ours_model.png}
\caption{Ground-truth vs. forecasted traffic volume time-series over test time steps for a representative camera node in the 608-node graph using TA-STGCN.}
\label{fig:node_prediction}
\end{figure*}

The visualization confirms that TA-STGCN tracks both peak traffic surges and off-peak declines with high fidelity, demonstrating tight alignment with physical traffic volume dynamics across extended forecast horizons.

\subsection{Temporal Attention Explainability Analysis}
\label{sec:attention_explainability}

To evaluate the internal interpretability and operational dynamics of TA-STGCN, we extract and analyze the temporal attention weight matrices $\mathbf{A} \in \mathbb{R}^{T_{\text{in}} \times T_{\text{in}}}$ computed by the Model-Level Multi-Head Temporal Self-Attention layer ($T_{\text{in}} = 24$ observation steps, corresponding to a 120-minute historical horizon). Figure~\ref{fig:attention_comparison} provides a comparative visualization of global attention heatmaps between the Evening Rush Hour (16:30--18:30) and the Night Off-Peak period (02:00--04:00), along with the differential attention matrix $\Delta \mathbf{A} = \mathbf{A}_{\text{evening}} - \mathbf{A}_{\text{offpeak}}$.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{fig/attention_comparison_evening_peak.pdf}
\caption{Global Temporal Attention Heatmap comparison: (a) Evening Rush Hour (16:30--18:30), (b) Night Off-Peak (02:00--04:00), and (c) Differential Heatmap $\Delta \mathbf{A} = \mathbf{A}_{\text{evening}} - \mathbf{A}_{\text{offpeak}}$. The horizontal axis denotes historical observation steps relative to forecast onset ($t-120\text{m}$ to $t-5\text{m}$), while the vertical axis indicates query sequence steps.}
\label{fig:attention_comparison}
\end{figure*}

\begin{table}[h]
\centering
\caption{Quantitative Temporal Attention Allocation across 5 Representative Urban Traffic Regimes.}
\label{tab:attention_quantitative}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Urban Traffic Regime Window} & \textbf{Recent ($t-20\text{m}$ to $t$)} & \textbf{Long-term ($t-120\text{m}$ to $t-60\text{m}$)} & \textbf{Ratio (Recent/Long)} \\
\midrule
Night Off-Peak (02:00 -- 04:00) & 0.0366 & 0.0440 & 0.83$\times$ \\
Morning Peak (07:30 -- 09:30) & 0.0376 & 0.0453 & 0.83$\times$ \\
Noon Normal (11:30 -- 13:30) & 0.0407 & 0.0410 & 0.99$\times$ \\
\textbf{Evening Peak (16:30 -- 18:30)} & \textbf{0.0428} & \textbf{0.0406} & \textbf{1.05$\times$} \\
Late Evening (21:00 -- 23:00) & 0.0434 & 0.0402 & 1.08$\times$ \\
\bottomrule
\end{tabular}%
}
\end{table}

Table~\ref{tab:attention_quantitative} summarizes the quantitative allocation of attention weights across five distinct operational windows throughout the day. Empirical analysis yields two primary methodological findings:
\begin{enumerate}
    \item \textbf{Regime-Dependent Temporal Receptive Adaptation:} During uncongested off-peak hours and early morning windows (02:00--04:00 and 07:30--09:30), the ratio of recent-to-long-term attention allocation remains at $0.83\times$, indicating that the model incorporates extended historical horizons ($60$--$120$ minutes prior) to establish steady-state baseline flows. As network traffic transitions into heavy evening congestion (16:30--18:30) and late evening regimes, this ratio monotonically escalates to $\mathbf{1.05\times}$ and $1.08\times$. This quantitative transition confirms that under congested conditions, the temporal self-attention module dynamically contracts its receptive focus toward immediate temporal momentum ($t-5\text{m}$ to $t-20\text{m}$) to capture rapid, non-linear bottleneck queue propagation.
    \item \textbf{Empirical Verification via Differential Attentional Mapping:} As visually evidenced in Figure~\ref{fig:attention_comparison}(c), the differential heatmap reveals a pronounced, positive vertical band along the recent time horizon ($\Delta \mathbf{A} > +0.015$, rendered in deep red). This positive differential demonstrates a statistically significant reallocation of attention weight toward recent steps during evening peak hours relative to night off-peak baselines, providing rigorous empirical confirmation that TA-STGCN dynamically adjusts its temporal attention span according to real-time traffic non-stationarity.
\end{enumerate}

\subsection{Ablation Study}

To isolate the contribution of each structural component within TA-STGCN, Table~\ref{tab:ablation_study} presents the comprehensive ablation study evaluated across 4 independent random seeds ($42, 100, 2024, 777$). We evaluate four distinct architectural variants:
\begin{enumerate}
    \item \textbf{TA-STGCN (Full Model):} The baseline full architecture featuring 2 ST-Conv blocks ($C=64$, $h=4$ heads).
    \item \textbf{TA-STGCN w/o Temporal Attention:} Completely removes the temporal self-attention module, reverting to a purely convolutional STGCN backbone.
    \item \textbf{TA-STGCN w/ Single-Head Attn ($h=1$):} Replaces multi-head attention ($h=2$) with a single-head attention mechanism ($h=1$).
    \item \textbf{TA-STGCN w/ Light Hidden Dim ($C=32$):} Reduces hidden channel dimension from 64 to 32 to evaluate model resilience under extreme parameter compression.
\end{enumerate}

\begin{table}[h]
\centering
\caption{Ablation Study Results for TA-STGCN Architectural Components across 4 Independent Random Seeds (Mean $\pm$ Std).}
\label{tab:ablation_study}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
\textbf{Ablation Model Variant} & \textbf{Params} & \textbf{MAE Overall} & \textbf{MAPE Overall} & \textbf{RMSE Overall} \\
\midrule
TA-STGCN (Full Model) & 315,910 & 3.2718 $\pm$ 0.0155 & 11.60\% $\pm$ 0.13\% & 4.4499 $\pm$ 0.0266 \\
w/o Temporal Attention & 265,926 & 3.2755 $\pm$ 0.0022 & 11.66\% $\pm$ 0.11\% & 4.4417 $\pm$ 0.0049 \\
\textbf{w/ Single-Head Attn ($h=1$)} & 315,910 & \textbf{3.2387 $\pm$ 0.0186} & \textbf{11.54\% $\pm$ 0.12\%} & 4.3961 $\pm$ 0.0248 \\
\textbf{w/ Light Hidden Dim ($C=32$)} & \textbf{121,094} & 3.2408 $\pm$ 0.0177 & 11.57\% $\pm$ 0.12\% & \textbf{4.3924 $\pm$ 0.0196} \\
\bottomrule
\end{tabular}%
}
\end{table}

The empirical results in Table~\ref{tab:ablation_study} yield three core insights regarding spatio-temporal forecasting under mixed, motorcycle-dominant traffic:
\begin{itemize}
    \item \textbf{Necessity of Temporal Attention Gating:} Removing the temporal self-attention module (\emph{w/o Temporal Attention}) yields the highest overall forecasting error ($\text{MAE} = 3.2755$, $\text{MAPE} = 11.66\%$), confirming that model-level temporal self-attention is essential for dynamic noise filtering and long-range dependency capture.
    \item \textbf{Attention Head Capacity and Regularization ($h=1$ vs. $h=2$):} The single-head attention variant ($h=1$) achieves the top overall forecasting precision ($\text{MAE} = 3.2387$, $\text{MAPE} = 11.54\%$). Under highly non-stationary mixed traffic conditions, allocating full channel capacity into a unified single-head attention projection prevents over-parameterization per head, acting as a structural regularizer against transient camera noise.
    \item \textbf{Extreme Parameter Efficiency ($C=32$):} Compressing the hidden channels to $C=32$ produces competitive performance ($\text{MAE} = 3.2408$, $\text{MAPE} = 11.57\%$, $\text{RMSE} = 4.3924$)—virtually matching the top single-head result—while attaining a **61.7\% parameter reduction** (121,094 vs. 315,910 parameters). This demonstrates that the proposed TA-STGCN framework maintains high precision under tight memory budgets, offering a viable model for edge computing deployments in real-world ITS networks.
\end{itemize}

\subsection{Comparative Discussion and Limitations}

\subsubsection{Forecasting Accuracy Across Horizons}
As shown in Table~\ref{tab:benchmark_5seeds}, \textbf{TA-STGCN} consistently outperforms both GCN-LSTM \cite{gcn, tgcn} and standard STGCN \cite{stgcn} across every single forecast horizon ($t+1$ to $t+6$).
\begin{itemize}
    \item \textbf{GCN-LSTM vs. TA-STGCN:} GCN-LSTM exhibits the highest forecasting error ($\text{MAE} = 8.5240$, $\text{RMSE} = 14.8250$) despite possessing the largest parameter count. The sequential nature of LSTM backpropagation creates a bottleneck when modeling complex 24-step historical dependencies across 608 nodes.
    \item \textbf{STGCN Baseline vs. TA-STGCN:} Standard STGCN achieves lower error than GCN-LSTM ($\text{MAE} = 3.2125$) due to parallel 1D gated temporal convolutions. However, because local 1D convolutions lack global temporal receptive fields, STGCN's error increases as the forecast horizon extends to $t+6$ ($\text{MAE}_{t+6} = 3.4345$). By incorporating Model-Level Multi-Head Temporal Self-Attention, TA-STGCN reduces overall error to $\mathbf{3.1923}$ and $t+6$ error to $\mathbf{3.4329}$.
    \item \textbf{Variance and Training Stability:} TA-STGCN exhibits lower standard deviation across the 5 random seeds ($\pm 0.0112$ MAE overall compared to $\pm 0.0151$ for STGCN Baseline). This suggests that Model-Level Multi-Head Temporal Self-Attention enhances training stability and increases resilience against non-stationary temporal noise.
\end{itemize}

\subsubsection{Architectural Efficiency and Parameter Economy}
A critical finding of this study is that TA-STGCN achieves its forecasting performance with the most lightweight parameter capacity among the evaluated models: a \textbf{54.6\% parameter reduction} compared to GCN-LSTM and a \textbf{45.6\% parameter reduction} compared to STGCN Baseline (Table~\ref{tab:model_comparison}). This evidence indicates that TA-STGCN's forecasting accuracy stems from architectural design—specifically coupling Chebyshev Graph Convolutions with Model-Level Multi-Head Temporal Self-Attention—rather than parameter scaling.

% --- NEW (Round 1): Limitations paragraph (skeleton; to be expanded in R2) ---
\subsubsection{Limitations}
Several limitations of the present study should be acknowledged. First, the ground-truth traffic volumes used to train and evaluate the Sub-problem 2 forecasting models are themselves predictions produced by the Stage 1 vision model rather than independently measured traffic counts; the forecasting benchmark therefore measures fidelity to camera-derived pseudo-labels, and the propagation of Stage 1 estimation error (MAE $=3.53$ vehicles per frame) into the network-wide forecasts is not yet quantified. Second, the comparative evaluation is limited to two baseline architectures; stronger attention-based STGNNs such as Graph WaveNet \cite{wavenet}, ASTGCN \cite{astgcn}, and GMAN \cite{gman} were not evaluated on this dataset, and the improvement margin over standard STGCN, while consistent across horizons and seeds, is small relative to typical architectural gaps in the literature. Third, the dataset covers a limited collection period from a single city, so the generalizability of the findings across seasons, weather regimes, and other urban road networks remains to be established. We view addressing these limitations—particularly quantifying two-stage error propagation and benchmarking against stronger baselines—as important directions for extending this work.
% TODO-R2: expand with significance-testing outcome; add data collection period
% once documented in Sec. III-A.

% =====================================================================
% SECTION V: CONCLUSION AND FUTURE WORK
% (Old Section VII; two mandatory fixes applied: "MAE ~ 5.0" -> 3.53, and
% "state-of-the-art" removed. Other content preserved.)
% =====================================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}

This paper introduced an end-to-end framework for urban traffic monitoring and multi-horizon forecasting, evaluated on a real-world camera network in Ho Chi Minh City, Vietnam. The system decouples traffic management into two sequential sub-problems: (1) camera-level vehicle count estimation, and (2) network-wide spatio-temporal graph forecasting.

% NOTE (Round 1): corrected "MAE ~ 5.0" to the reported test MAE of 3.53
% (consistent with Table II and the Abstract).
For Sub-problem 1, a ConvNeXt-Tiny direct regression network was trained on 4,000 images, validated on 1,000, and tested on 1,000 images, achieving robust vehicle count accuracy (MAE $= 3.53$ vehicles per frame).
% TODO-R2: the original text claimed the 6,000 images span "608 camera
% locations", which conflicts with the dataset description; restate the actual
% number of contributing cameras once confirmed.
For Sub-problem 2, estimated vehicle counts were structured into a 608-node directed traffic graph $\mathcal{G} = (\mathcal{V}, \mathcal{E}, W)$ built with RBF-weighted road distances. Addressing the fundamental flaws of single-node independent forecasting—which ignores spatial traffic spillover—we introduced \textbf{TA-STGCN} (Temporal Attention-Guided Spatio-Temporal Graph Convolutional Network), combining Chebyshev Graph Convolutions ($K=3$), 1D Gated Temporal Convolutions (GLU), and Model-Level Multi-Head Temporal Self-Attention.

% NOTE (Round 1): "state-of-the-art" replaced with a claim scoped to the
% evaluated baselines.
Comprehensive multi-seed evaluations across 4 independent random seeds ($42, 100, 2024, 777$) demonstrated that TA-STGCN outperforms the evaluated baselines in multi-horizon forecasting ($\text{MAE} = 3.1923 \pm 0.0112$, $\text{RMSE} = 4.3435 \pm 0.0142$) across all 6 prediction horizons ($t+1$ to $t+6$, 5-to-30 minutes ahead). Notably, TA-STGCN achieves these results with a significantly more lightweight parameter capacity (165,318 parameters) compared to traditional GCN-LSTM (364,198 parameters) and standard STGCN (303,846 parameters).

Future work will explore incorporating dynamic spatial graph learning to capture temporal topological shifts during major road incidents and extreme weather events.

\begin{thebibliography}{99}

\bibitem{yolo}
J. Redmon, S. Divvala, R. Girshick, and A. Farhadi, ``You only look once: Unified, real-time object detection,'' in \emph{Proc. IEEE Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2016, pp. 779--788.

\bibitem{mcnn}
Y. Zhang, D. Zhou, S. Chen, S. Gao, and Y. Ma, ``Single-image crowd counting via multi-column convolutional neural network,'' in \emph{Proc. IEEE Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2016, pp. 589--597.

\bibitem{csrnet}
Y. Li, X. Zhang, and D. Chen, ``CSRNet: Dilated convolutional neural networks for understanding the highly congested scenes,'' in \emph{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2018, pp. 1091--1100.

\bibitem{can}
S. Bai, Z. He, Y. Wu, and D. Wang, ``Context-aware crowd counting,'' in \emph{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2019, pp. 6909--6918.

\bibitem{resnet}
K. He, X. Zhang, S. Ren, and J. Sun, ``Deep residual learning for image recognition,'' in \emph{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2016, pp. 770--778.

\bibitem{convnext}
Z. Liu, H. Mao, C. Y. Wu, C. Feichtenhofer, T. Darrell, and S. Xie, ``A convnet for the 2020s,'' in \emph{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2022, pp. 11976--11986.

\bibitem{arima}
G. E. P. Box, G. M. Jenkins, G. C. Reinsel, and G. M. Ljung, \emph{Time Series Analysis: Forecasting and Control}, 5th ed. Hoboken, NJ, USA: Wiley, 2015.

\bibitem{svr}
H. Drucker, C. J. Burges, L. Kaufman, A. J. Smola, and V. Vapnik, ``Support vector regression machines,'' in \emph{Adv. Neural Inf. Process. Syst. (NeurIPS)}, vol. 9, 1996, pp. 155--161.

\bibitem{ctm}
C. F. Daganzo, ``The cell transmission model: A dynamic representation of highway traffic consistent with the hydrodynamic theory,'' \emph{Transp. Res. Part B: Methodol.}, vol. 28, no. 4, pp. 269--287, 1994.

\bibitem{gcn}
T. N. Kipf and M. Welling, ``Semi-supervised classification with graph convolutional networks,'' in \emph{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2017, pp. 1--14.

\bibitem{chebnet}
M. Defferrard, X. Bresson, and P. Vandergheynst, ``Convolutional neural networks on graphs with fast localized spectral filtering,'' in \emph{Adv. Neural Inf. Process. Syst. (NeurIPS)}, vol. 29, 2016, pp. 3844--3852.

\bibitem{stgcn}
B. Yu, H. Yin, and Z. Zhu, ``Spatio-temporal graph convolutional networks: A deep learning framework for traffic forecasting,'' in \emph{Proc. 27th Int. Joint Conf. Artif. Intell. (IJCAI)}, 2018, pp. 3634--3640.

\bibitem{dcrnn}
Y. Li, R. Yu, C. Shahabi, and Y. Liu, ``Diffusion convolutional recurrent neural network: Data-driven traffic forecasting,'' in \emph{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2018, pp. 1--16.

\bibitem{tgcn}
L. Zhao, Y. Song, C. Zhang, Y. Liu, P. Wang, T. Lin, M. Deng, and H. Li, ``T-GCN: A temporal graph convolutional network for traffic prediction,'' \emph{IEEE Trans. Intell. Transp. Syst.}, vol. 21, no. 9, pp. 3848--3858, 2019.

% TODO-R2: verify this reference against its DOI (existence/metadata unconfirmed).
\bibitem{gclstm}
Z. Lu, X. Long, J. Song, and M. Geng, ``Graph convolutional LSTM network for traffic flow prediction,'' \emph{IEEE Access}, vol. 8, pp. 100980--100988, 2020.

\bibitem{wavenet}
Z. Wu, S. Pan, F. Chen, G. Long, C. Zhang, and P. S. Yu, ``Graph WaveNet for deep spatial-temporal graph modeling,'' in \emph{Proc. 28th Int. Joint Conf. Artif. Intell. (IJCAI)}, 2019, pp. 1907--1913.

\bibitem{attention}
A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez, L. Kaiser, and I. Polosukhin, ``Attention is all you need,'' in \emph{Adv. Neural Inf. Process. Syst. (NeurIPS)}, vol. 30, 2017, pp. 5998--6008.

\bibitem{gman}
C. Zheng, X. Fan, C. Wang, and J. Qi, ``GMAN: A graph multi-attention network for traffic prediction,'' in \emph{Proc. AAAI Conf. Artif. Intell. (AAAI)}, vol. 34, no. 01, 2020, pp. 1234--1241.

\bibitem{agcrn}
L. Bai, L. Yao, C. Li, X. Wang, and C. Wang, ``Adaptive graph convolutional recurrent network for traffic forecasting,'' in \emph{Adv. Neural Inf. Process. Syst. (NeurIPS)}, vol. 33, 2020, pp. 17804--17815.

\bibitem{astgcn}
S. Guo, Y. Lin, N. Feng, C. Song, and H. Wan, ``Attention based spatial-temporal graph convolutional networks for traffic flow forecasting,'' in \emph{Proc. AAAI Conf. Artif. Intell. (AAAI)}, vol. 33, no. 01, 2019, pp. 922--929.

% NOTE (Round 1): corrected title and page range of STSGCN (AAAI 2020).
\bibitem{stsgcn}
C. Song, Y. Lin, S. Guo, and H. Wan, ``Spatial-temporal synchronous graph convolutional networks: A new framework for spatial-temporal network data forecasting,'' in \emph{Proc. AAAI Conf. Artif. Intell. (AAAI)}, vol. 34, no. 01, 2020, pp. 914--921.

% NOTE (Round 1): publication year corrected to 2021 (vol. 32, no. 1).
\bibitem{gnn_survey}
Z. Wu, S. Pan, F. Chen, G. Long, C. Zhang, and P. S. Yu, ``A comprehensive survey on graph neural networks,'' \emph{IEEE Trans. Neural Netw. Learn. Syst.}, vol. 32, no. 1, pp. 4--24, 2021.

\bibitem{huber}
P. J. Huber, ``Robust estimation of a location parameter,'' \emph{Ann. Math. Statist.}, vol. 35, no. 1, pp. 73--101, 1964.

\bibitem{adam}
D. P. Kingma and J. Ba, ``Adam: A method for stochastic optimization,'' in \emph{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2015, pp. 1--15.

\bibitem{vnlstm}
V. M. Do, Q. H. Tran, K. G. Le, X. C. Vuong, and V. T. Vu, ``Enhanced deep neural networks for traffic speed forecasting regarding sustainable traffic management using probe data from registered transport vehicles on multilane roads,'' \emph{Sustainability}, vol. 16, no. 6, p. 2453, 2024.
\bibitem{yin_survey}
X. Yin, G. Wu, J. Wei, Y. Shen, H. Qi, and B. Yin, ``Deep learning on traffic prediction: Methods, analysis and future directions,'' \emph{IEEE Trans. Intell. Transp. Syst.}, vol. 23, no. 6, pp. 4927--4943, 2022.

\bibitem{magcrn}
A. Zeb, Y. Ye, S. Zhang, and J. J. Q. Yu, ``Meta attentive graph convolutional recurrent network for traffic forecasting,'' arXiv preprint arXiv:2308.14377, 2023.

\end{thebibliography}

\end{document}